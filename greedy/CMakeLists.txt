compile_benchmark(activity_selection)
compile_test(activity_selection)

compile_benchmark(array_of_doubled_pairs)
compile_test(array_of_doubled_pairs)

compile_benchmark(assign_cookies)
compile_test(assign_cookies)

compile_benchmark(boats_to_save_people)
compile_test(boats_to_save_people)

compile_benchmark(break_a_palindrome)
compile_test(break_a_palindrome)

compile_benchmark(broken_calculator)
compile_test(broken_calculator)

compile_benchmark(candy)
compile_test(candy)

compile_benchmark(can_place_flowers)
compile_test(can_place_flowers)

compile_benchmark(DI_string_match)
compile_test(DI_string_match)

compile_benchmark(gas_station)
compile_test(gas_station)

compile_benchmark(huffman_codes)
compile_test(huffman_codes)

compile_benchmark(jump_game)
compile_test(jump_game)

compile_benchmark(max_sum_after_k_negations)
compile_test(max_sum_after_k_negations)

compile_benchmark(min_domino_rotations_for_equal_row)
compile_test(min_domino_rotations_for_equal_row)

compile_benchmark(min_operations_to_make_array_increasing)
compile_test(min_operations_to_make_array_increasing)

compile_benchmark(minimize_deviation_in_array)
compile_test(minimize_deviation_in_array)

compile_benchmark(offline_caching)
compile_test(offline_caching)

compile_benchmark(patching_array)
compile_test(patching_array)

compile_benchmark(reduce_array_size_to_half)
compile_test(reduce_array_size_to_half)

compile_benchmark(remove_duplicate_letters)
compile_test(remove_duplicate_letters)

compile_benchmark(remove_k_digits)
compile_test(remove_k_digits)

compile_benchmark(smallest_str_with_given_value)
compile_test(smallest_str_with_given_value)

compile_benchmark(task_scheduling)
compile_test(task_scheduling)

compile_benchmark(two_city_scheduling)
compile_test(two_city_scheduling)

compile_benchmark(valid_triangle_number)
compile_test(valid_triangle_number)
