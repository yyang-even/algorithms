compile_benchmark(are_rectangles_overlap)
compile_test(are_rectangles_overlap)

compile_benchmark(circle_drawing)
compile_test(circle_drawing)

compile_benchmark(clock_angle)
compile_test(clock_angle)

compile_benchmark(convex_hull)
compile_test(convex_hull)

compile_benchmark(cut_two_squares_in_half)
compile_test(cut_two_squares_in_half)

compile_benchmark(find_max_points_line)
compile_test(find_max_points_line)

compile_benchmark(flood_fill)
compile_test(flood_fill)

compile_benchmark(is_point_inside_rectangle)
compile_test(is_point_inside_rectangle)

compile_benchmark(largest_triangle_area)
compile_test(largest_triangle_area)

compile_benchmark(projection_area)
compile_test(projection_area)

compile_benchmark(slope_of_line)
compile_test(slope_of_line)

compile_benchmark(surface_area_of_3D_shapes)
compile_test(surface_area_of_3D_shapes)

compile_benchmark(two_lines_intersect)
compile_test(two_lines_intersect)
