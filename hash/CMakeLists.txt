compile_benchmark(are_two_sets_disjoint)
compile_test(are_two_sets_disjoint)

compile_benchmark(a_pair_elements_product_as_n)
compile_test(a_pair_elements_product_as_n)

compile_benchmark(a_pair_elements_sum_as_n)
compile_test(a_pair_elements_sum_as_n)

compile_benchmark(a_pair_primes_product_as_n)
compile_test(a_pair_primes_product_as_n)

compile_benchmark(a_pair_primes_sum_as_n)
compile_test(a_pair_primes_sum_as_n)

compile_benchmark(a_pair_sum_as_n_from_two_array)
compile_test(a_pair_sum_as_n_from_two_array)

compile_benchmark(check_all_elements_distinct)
compile_test(check_all_elements_distinct)

compile_benchmark(check_if_one_string_can_be_formed_from_another)
compile_test(check_if_one_string_can_be_formed_from_another)

compile_benchmark(common_characters)
compile_test(common_characters)

compile_benchmark(conditionally_assign)
compile_test(conditionally_assign)

compile_benchmark(count_frequencies_of_all_elements)
compile_test(count_frequencies_of_all_elements)

compile_benchmark(elements_that_occurred_only_once)
compile_test(elements_that_occurred_only_once)

compile_benchmark(find_first_nonrepeating_char)
compile_test(find_first_nonrepeating_char)

compile_benchmark(find_first_repeating_element)
compile_test(find_first_repeating_element)

compile_benchmark(find_kth_nonrepeating_char)
compile_test(find_kth_nonrepeating_char)

compile_benchmark(find_overlapping_sum_of_two_array)
compile_test(find_overlapping_sum_of_two_array)

compile_benchmark(find_second_frequent_char)
compile_test(find_second_frequent_char)

compile_benchmark(last_occurrence_of_elements_in_relative_order)
compile_test(last_occurrence_of_elements_in_relative_order)

compile_benchmark(majority_element)
compile_test(majority_element)

compile_benchmark(maximum_occurring_character)
compile_test(maximum_occurring_character)

compile_benchmark(print_items_containing_all_chars_of_given_word)
compile_test(print_items_containing_all_chars_of_given_word)

compile_benchmark(rearrange_array_arrj_becomes_i_if_arri_j)
compile_test(rearrange_array_arrj_becomes_i_if_arri_j)

compile_benchmark(rearrange_array_arr_arr_i)
compile_test(rearrange_array_arr_arr_i)

compile_benchmark(remove_duplicates)
compile_test(remove_duplicates)

compile_benchmark(repeated_elements_in_array)
compile_test(repeated_elements_in_array)

compile_benchmark(top_k_most_frequent_numbers)
compile_test(top_k_most_frequent_numbers)
