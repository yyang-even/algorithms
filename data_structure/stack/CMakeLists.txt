compile_benchmark(132_pattern)
compile_test(132_pattern)

compile_benchmark(basic_calculator)
compile_test(basic_calculator)

compile_benchmark(check_balanced_parentheses)
compile_test(check_balanced_parentheses)

compile_benchmark(crawler_log_folder)
compile_test(crawler_log_folder)

compile_benchmark(decode_string)
compile_test(decode_string)

compile_test(k_stack)

compile_benchmark(make_string_great)
compile_test(make_string_great)

compile_benchmark(maximum_frequency_stack)
compile_test(maximum_frequency_stack)

compile_test(min_stack)

compile_benchmark(next_greater_element)
compile_test(next_greater_element)

compile_benchmark(recursively_remove_adjacent_duplicates)
compile_test(recursively_remove_adjacent_duplicates)

compile_test(sort_stack)

compile_test(stack)

compile_benchmark(validate_stack_sequences)
compile_test(validate_stack_sequences)
